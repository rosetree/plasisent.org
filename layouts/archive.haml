%h1= item[:title]

- if item[:posts]
  %ol
    - item[:posts].each do |i|
      %li<
        %em= i.linked_date_information
        %br/
        = link_to i[:title], i.path

-#%h2 Neue Themen -# TODO
 
- if item[:referenced_tags]
  %h2 Angesprochene Themen
  %p= link_tags item, :referenced_tags
