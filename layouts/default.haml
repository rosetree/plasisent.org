!!!
%html{lang: "de"}
  %head
    %meta{charset: "utf-8"}
    %title #{@item[:title]} | Plasisent
    %link{rel: "stylesheet", href: 'http://fonts.googleapis.com/css?family=Ubuntu+Mono:400,700|Ubuntu:400,700,400italic,700italic'}
    %link{rel: "stylesheet", href: @items["/assets/styles/master/"].path}
    %link{rel: "alternate",  href: @items["/feed/"].path, type: "application/atom+xml"}
    - unless @item.github_url == ""
      %link{rel: "alternate",  href: @item.github_url, title: "Source Code on GitHub."}
    %link{rel: "profile", href: "http://osprotocol.com"}
    %meta{property: "os:repo", content: "https://github.com/rosetree/plasisent.org"}
    %meta{property: "os:rcs_type", content: "git"}
    %meta{property: "os:src", content: "git@github.com:rosetree/plasisent.org"}
    %meta{name: "viewport",  content: "width=device-width,initial-scale=1"}
    %meta{name: "generator", content: "nanoc #{Nanoc::VERSION}"}
    - if item[:prev] or item[:next]
      - if item[:prev]
        %link{rel: "prev", title: item[:prev][:title], href: item[:prev].path}
      - if item[:next]
        %link{rel: "next", title: item[:next][:title], href: item[:next].path}
  %body

    %h1= link_to "Plasisent", @items["/"].path

    - if @item[:cleaned?] == false
      = load_snippet("Warnung Unbereinigter Import") % link_to("Datei auf GitHub", @item.github_url)

    -#
      After the header is done, load a different layout for each kind of
      content you want to create. The kind could be one of the following:

      * page
      * article
      * category / tag
      * quicky
      * archive

    %main{role: "main"}

      - case @item[:kind]
      - when "category"
        - category_items = all_items_with_category item[:name] || item[:title]
        = render "category", :category_items => category_items do
          = yield
      - when "tag"
        = render "tag" do
          = yield
      - when "archive"
        = render "archive" do
          yield
      - when "article"
        = render "article" do
          ~ yield
      - else
        ~ yield


    %footer{role: "contentinfo"}
      -# FIXME: Why am I using `item` instead of `@item` from here?
      - if item[:prev] or item[:next]
        %p
          - if item[:prev]
            #{link_to item[:prev][:title], item[:prev].path}
            ←
          - if item[:next]
            → #{link_to item[:next][:title], item[:next].path}

      - category_link = ""
      - if item[:category]
        - category_link = link_category item

      - if item[:tags] && item[:tags] != [] or category_link != "" or @item.github_url != ""
        %h2{title: "Metadaten von „#{item[:title]}“."} Metadaten

        %p

          - if category_link != ""
            #{link_to @item[:title], @item.path} ist eingeordnet im Regal #{category_link}.

          - if @item[:tags] && @item[:tags] != []
            Dieser Beitrag behandelt
            - if @item[:tags].count == 1
              das Thema
            - else
              die Themen
            #{link_tags item}.

          - unless @item.github_url == ""
            Den Quelltext dieser Seite findest du auf #{link_to "GitHub", @item.github_url}.

      %h2 Sonst noch was?

      = load_snippet "Follow Me"
