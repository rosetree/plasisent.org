---
title: "Ein mehr oder weniger politisches Blog"
---

Herzlich Willkommen auf meiner Blogwiese. Schau dich ruhig um, machs dir
bequem, nehm’ dir einen Kaffee. Ich spreche hier ein paar Themen an, die mir
wichtig sind. Was mich aktuell so beschäftigt siehst du direkt unter diesem
kurzen Absatz.

<% count = 4 %>
<% index = 0 %>

<% sorted_articles.each do |article| %>

<% # TODO: This code that creates links to the archives is nearly the same, as somewhere else. %>
<% idate = article[:created_at] %>
<% year_link = link_to idate.strftime("%Y"), "/#{idate.strftime("%Y")}/" %>
<% week_link = link_to "Woche #{idate.strftime("%-V")}", "/#{idate.strftime("%Y")}/kw-#{idate.strftime("%V")}/" %>

# <%= link_to article[:title], article.path %>

*Geschrieben am <%= idate.strftime("%-d %B") %> (<%= year_link %>, <%= week_link %>).*

<%= article.compiled_content %>

<% index += 1 %>
<% break if index == count %>

<% end %>
